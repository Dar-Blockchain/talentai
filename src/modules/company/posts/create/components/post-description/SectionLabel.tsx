const SectionLabel = ({ children }: { children: React.ReactNode }) => (
  <p className="mb-2.5 text-[10.5px] font-bold uppercase tracking-[0.07em] text-[#9CA3AF]">
    {children}
  </p>
);

export default SectionLabel;
