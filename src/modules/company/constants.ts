export const TEAL        = "#0D9488";
export const TEAL_BG     = "#F0FDFA";
export const TEAL_BORDER = "#99F6E4";
export const PURPLE      = "#8310FF";

// Brand colors, matched to the TalentAI logo (see the theme-tokens comment
// block in src/styles/globals.css — same hexes as --color-primary /
// --color-primary-dark). Single source of truth for modules that need the
// raw hex for alpha-blended inline styles; prefer the bg-primary/text-primary
// Tailwind utilities elsewhere so these can't drift from the CSS tokens.
export const ACCENT      = "#6AD39C"; // primary mint green — logo circle
export const ACCENT_DARK = "#10453F"; // deep green — logo wordmark ink
