/** Matches Backend `LAST_MESSAGE_DELETED_SENTINEL` (team chat lastMessage preview). */
export const TEAM_LAST_MESSAGE_DELETED_SENTINEL = "__DELETED__";

/** Matches Backend `TEAM_MESSAGE_BODY_TOMBSTONE` (stored body after delete-for-everyone). */
export const TEAM_MESSAGE_BODY_TOMBSTONE = "__TEAM_MESSAGE_DELETED__";
