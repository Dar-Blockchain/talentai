// SkillInterviewAssessmentData is defined locally in SkillInterviewAssessments.tsx
// since its shape is specific to that component's API response.

export interface AdminSkillInterviewStats {
  total: number;
  excellent: number;
  satisfactory: number;
  needsWork: number;
  archived: number;
}
