// Re-export from the canonical implementation so all callers share one instance.
export type { NotificationType } from '@/utils/notificationSounds';
export { notificationSound, playNotificationSound } from '@/utils/notificationSounds';
