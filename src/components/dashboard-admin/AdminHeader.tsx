import { Typography, Box } from '@mui/material';

const AdminHeader = () => (
  <Box sx={{ mb: 4 }}>
    <Typography variant="h3" fontWeight={900} color="#8310FF" gutterBottom>
      Admin Dashboard
    </Typography>
  </Box>
);

export default AdminHeader; 