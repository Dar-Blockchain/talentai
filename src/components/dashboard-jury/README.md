# Dashboard Jury Components

This folder contains reusable and isolated subcomponents for the Jury Dashboard page (`dashboardJury.tsx`).

Move and maintain all Jury Dashboard-specific UI components here to keep the main page clean and maintainable. 